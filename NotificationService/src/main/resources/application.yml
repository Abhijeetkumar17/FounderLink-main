server:
  port: 8087

spring:
  application:
    name: notification-service
  config:
    import: optional:configserver:http://localhost:8888
  datasource:
    url: jdbc:postgresql://localhost:5432/founderlink_notifications
    username: postgres
    password: Abhi9693
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: false
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${SPRING_MAIL_USERNAME:abhijeetsinha2017@gmail.com}
    password: ${SPRING_MAIL_PASSWORD:bpxd jbqx qqiy yfsr}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

rabbitmq:
  exchange: founderlink.exchange
  queue:
    investment-created: investment.created.queue
    investment-approved: investment.approved.queue
    team-invite-sent: team.invite.sent.queue
    team-invite-accepted: team.invite.accepted.queue
    team-invite-rejected: team.invite.rejected.queue
    startup-created: startup.created.queue
    startup-approved: startup.approved.queue
    startup-rejected: startup.rejected.queue
    payment-success: payment.success.queue
    payment-failed: payment.failed.queue
    user-registered: user.registered.queue

management:
  metrics:
    tags:
      application: ${spring.application.name}
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  endpoint:
    health:
      show-details: always
  tracing:
    sampling:
      probability: 1.0
  zipkin:
    tracing:
      endpoint: http://localhost:9411/api/v2/spans

jwt:
  secret: 5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437
  access-token-expiration: 86400000
  refresh-token-expiration: 604800000
